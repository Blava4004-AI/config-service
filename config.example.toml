# Homelab Config Service
# Copy to config.toml

[api]
port = 5195

[api.ssl]
key_path = ""
cert_path = ""

[api.auth]
# Set to require a bearer token for API access
require_token = false
token = ""

[auth]
# Set to true when running behind Mosaic with the Auth Service
# Set to false for standalone mode (no authentication)
enabled = false
min_permission = "admin"

[database]
driver = "better-sqlite3"
filename = "./data/config.db"

[frontend]
api_base_url = "http://localhost:5195"
